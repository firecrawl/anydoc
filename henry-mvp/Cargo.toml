[package]
name = "anydoc-henry-mvp"
version = "0.1.0"
edition = "2024"
rust-version = "1.88"

[dependencies]
anydoc = { path = ".." }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"

[dev-dependencies]
tempfile = "3"

# Keep this proof-of-concept crate isolated from the upstream workspace.
[workspace]
